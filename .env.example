PTCG_API_KEY=your_pokemontcg_api_key # (optional) external data source key
ADMIN_TOKEN=change_me_admin          # required: protect /admin/* endpoints
INGEST_TOKEN=change_me_ingest        # required: /admin/ingest/prices endpoint
PUBLIC_BASE_URL=https://pokequant.example.workers.dev # used for generated links
RESEND_API_KEY=your_resend_key       # (optional) email sending

# Optional retention override windows (days)
# RETENTION_BACKTESTS_DAYS=30
# RETENTION_METRICS_DAILY_DAYS=14
# RETENTION_FACTOR_RETURNS_DAYS=120

# Future expansion placeholders
POSTGRES_URL=
REDIS_URL=
MEILISEARCH_HOST=
MEILISEARCH_KEY=
